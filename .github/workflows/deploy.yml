name: deploy

# On every push to master: run all unit tests, deploy the API to TEST, migrate
# the test DB, smoke-check it, then (auto) do the same for PROD. The storefront
# is deployed separately by Firebase App Hosting (two backends, test + prod),
# which watches this same repo — see ecommerce-storefront/apphosting*.yaml.

on:
  push:
    branches: [master]
  workflow_dispatch: {}

permissions:
  contents: read
  id-token: write # required for Workload Identity Federation

concurrency:
  group: deploy-${{ github.ref }}
  cancel-in-progress: false

env:
  REGION: us-east4
  SERVICE: ninja-commerce-api
  TEST_PROJECT: ninja-commerce-test
  PROD_PROJECT: ninja-commerce-1d830

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: npm
          cache-dependency-path: '**/package-lock.json'
      - name: API unit tests
        working-directory: ecommerce-api
        run: npm ci && npm test
      - name: Storefront unit tests
        working-directory: ecommerce-storefront
        run: npm ci && npm test
      - name: Admin unit tests
        working-directory: ecommerce-admin
        run: npm ci && npm test

  deploy-test:
    needs: test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOY_SA_TEST }}
      - uses: google-github-actions/setup-gcloud@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      # Migrate BEFORE deploying new code (additive migrations are safe for the
      # briefly-old running instance; the new image then finds the schema ready).
      - name: Migrate test database
        working-directory: ecommerce-api
        env:
          DATABASE_URL: ${{ secrets.TEST_DATABASE_URL }}
        run: |
          npm ci
          npx prisma migrate deploy
      - name: Deploy API to test Cloud Run
        run: |
          gcloud run deploy "$SERVICE" \
            --source ecommerce-api \
            --project "$TEST_PROJECT" \
            --region "$REGION" \
            --quiet
      - name: Smoke test (test API)
        run: |
          URL=$(gcloud run services describe "$SERVICE" --project "$TEST_PROJECT" --region "$REGION" --format='value(status.url)')
          echo "test API: $URL"
          curl -fsS --retry 5 --retry-connrefused "$URL/products" > /dev/null
          echo "✅ test API responding"
      - name: Build + deploy admin (test) → Firebase Hosting
        working-directory: ecommerce-admin
        env:
          # Public Firebase web config + Cloud Run URL — baked into the SPA at build.
          VITE_API_URL: https://ninja-commerce-api-808867399514.us-east4.run.app
          VITE_FIREBASE_API_KEY: AIzaSyC2yd_eQ0u1GapYnlpwBYoaO4rfs4fGdS4
          VITE_FIREBASE_AUTH_DOMAIN: ninja-commerce-test.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: ninja-commerce-test
          VITE_FIREBASE_STORAGE_BUCKET: ninja-commerce-test.firebasestorage.app
          VITE_USE_EMULATORS: "false"
        run: |
          npm ci
          npm run build
          npx firebase-tools deploy --only hosting --project "$TEST_PROJECT" --non-interactive

  deploy-prod:
    needs: deploy-test
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - id: auth
        uses: google-github-actions/auth@v2
        with:
          workload_identity_provider: ${{ vars.WIF_PROVIDER }}
          service_account: ${{ vars.DEPLOY_SA_PROD }}
      - uses: google-github-actions/setup-gcloud@v2
      - uses: actions/setup-node@v4
        with:
          node-version: 22
      - name: Migrate production database
        working-directory: ecommerce-api
        env:
          DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}
        run: |
          npm ci
          npx prisma migrate deploy
      - name: Deploy API to prod Cloud Run
        run: |
          gcloud run deploy "$SERVICE" \
            --source ecommerce-api \
            --project "$PROD_PROJECT" \
            --region "$REGION" \
            --quiet
      - name: Smoke test (prod API)
        run: |
          URL=$(gcloud run services describe "$SERVICE" --project "$PROD_PROJECT" --region "$REGION" --format='value(status.url)')
          echo "prod API: $URL"
          curl -fsS --retry 5 --retry-connrefused "$URL/products" > /dev/null
          echo "✅ prod API responding"
      - name: Build + deploy admin (prod) → Firebase Hosting
        working-directory: ecommerce-admin
        env:
          VITE_API_URL: https://ninja-commerce-api-1047378822080.us-east4.run.app
          VITE_FIREBASE_API_KEY: AIzaSyBlMgf95C3CXCTWo3aoKU17wczHcWrT5D4
          VITE_FIREBASE_AUTH_DOMAIN: ninja-commerce-1d830.firebaseapp.com
          VITE_FIREBASE_PROJECT_ID: ninja-commerce-1d830
          VITE_FIREBASE_STORAGE_BUCKET: ninja-commerce-1d830.firebasestorage.app
          VITE_USE_EMULATORS: "false"
        run: |
          npm ci
          npm run build
          npx firebase-tools deploy --only hosting --project "$PROD_PROJECT" --non-interactive
